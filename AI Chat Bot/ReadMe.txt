To run the attached code:

Ensure sentence_transformers and torch are installed, by running: pip install sentence-transformers torch
Ensure that the "Responses.txt" file (containing pre-generated chatbot responses) is included in the same directory as the main script.
